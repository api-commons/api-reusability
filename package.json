{
  "name": "api-reusability",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A browser-first tool for assessing API reusability across an organization — discover evidence of APIs (apis.io, GitHub, HAR uploads, gateways via helper), index them as OpenAPI in APIs.json, and score reusability per API and per org/team/domain.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "presets": "node tools/build-presets.mjs"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "monaco-editor": "^0.52.2",
    "yaml": "^2.6.1"
  }
}
